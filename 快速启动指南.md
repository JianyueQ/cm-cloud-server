# 微服务快速启动指南

## 前置准备

### 1. 安装Nacos

**下载地址**: https://github.com/alibaba/nacos/releases

**Windows启动**:
```bash
# 解压后进入bin目录,以单机模式启动
cd nacos/bin
startup.cmd -m standalone
```

**访问控制台**: http://localhost:8848/nacos
- 用户名: nacos
- 密码: nacos

### 2. 确认基础环境

- ✅ MySQL已启动 (3306端口)
- ✅ Redis已启动 (6379端口)
- ✅ Nacos已启动 (8848端口)
- ✅ RabbitMQ已启动 (5672端口,可选)

## 编译项目

```bash
# 进入项目根目录
cd C:\Users\31373\Desktop\cm-cloud-server

# 编译整个项目
mvn clean install -DskipTests
```

## 启动服务

### 方式一: IDEA启动(推荐)

按以下顺序启动各个服务的Application类:

1. **AuthApplication** (cm-auth) - 认证服务
2. **SystemApplication** (cm-system) - 系统管理
3. **UserApplication** (cm-user) - 用户服务
4. **CourseApplication** (cm-course) - 课程服务
5. **AttendanceApplication** (cm-attendance) - 考勤服务
6. **GradeApplication** (cm-grade) - 成绩服务
7. **ResourceApplication** (cm-resource) - 资源服务
8. **MessageApplication** (cm-message) - 消息服务
9. **GatewayApplication** (cm-gateway) - 网关服务(最后启动)

### 方式二: Maven命令启动

```bash
# 启动认证服务
cd cm-auth
mvn spring-boot:run

# 新开命令行,启动网关
cd cm-gateway
mvn spring-boot:run

# 依此类推...
```

### 方式三: JAR包启动

```bash
# 先打包
mvn clean package -DskipTests

# 启动各个服务
java -jar cm-auth/target/cm-auth-1.0-SNAPSHOT.jar
java -jar cm-gateway/target/cm-gateway-1.0-SNAPSHOT.jar
# ...
```

## 验证服务

### 1. 查看Nacos控制台

访问: http://localhost:8848/nacos

进入 **服务管理 → 服务列表**,应该能看到以下服务:

- cm-gateway
- cm-auth
- cm-system
- cm-user
- cm-course
- cm-attendance
- cm-grade
- cm-resource
- cm-message

### 2. 测试网关路由

```bash
# 测试网关是否正常
curl http://localhost:8080/

# 通过网关访问认证服务
curl http://localhost:8080/auth/health

# 通过网关访问用户服务
curl http://localhost:8080/user/health
```

### 3. 直接访问微服务

```bash
# 直接访问认证服务
curl http://localhost:8081/health

# 直接访问用户服务
curl http://localhost:8083/health
```

## 服务健康检查

每个服务都提供健康检查端点:

```
http://localhost:8081/actuator/health   # 认证服务
http://localhost:8082/actuator/health   # 系统管理
http://localhost:8083/actuator/health   # 用户服务
...
```

## 常见问题

### 1. 服务无法注册到Nacos

**检查清单**:
- Nacos是否正常启动
- 配置文件中Nacos地址是否正确
- 网络是否畅通
- 防火墙是否开放8848端口

**解决方案**:
```yaml
# 检查application.yaml配置
spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  # 确保地址正确
```

### 2. 服务启动失败

**常见原因**:
- 端口被占用
- 数据库连接失败
- Redis连接失败
- 依赖冲突

**排查步骤**:
```bash
# 检查端口占用
netstat -ano | findstr "8081"

# 查看启动日志
# 检查异常信息
```

### 3. 网关路由失败

**检查配置**:
```yaml
# cm-gateway/src/main/resources/application.yaml
spring:
  cloud:
    gateway:
      routes:
        - id: cm-auth
          uri: lb://cm-auth  # lb:// 表示从Nacos负载均衡
          predicates:
            - Path=/auth/**
```

### 4. Feign调用失败

**确认事项**:
- 目标服务已注册到Nacos
- Feign接口定义正确
- @EnableFeignClients注解已添加

### 5. 配置文件不生效

**优先级顺序**:
1. bootstrap.yaml (最高优先级)
2. Nacos配置中心
3. application.yaml
4. 环境变量

## 开发建议

### 1. 单个服务调试

如果只想调试某个服务,可以注释掉网关路由,直接访问服务端口:

```
http://localhost:8081/xxx  # 直接访问认证服务
http://localhost:8083/xxx  # 直接访问用户服务
```

### 2. 热部署

在IDEA中开启热部署:

1. 添加spring-boot-devtools依赖
2. 设置: File → Settings → Build → Compiler → Build project automatically
3. 按 Ctrl+Shift+Alt+/ → Registry → compiler.automake.allow.when.app.running

### 3. 日志配置

在application.yaml中配置日志级别:

```yaml
logging:
  level:
    com.cm: DEBUG          # 业务代码日志
    com.alibaba.nacos: INFO  # Nacos日志
    org.springframework.cloud.gateway: DEBUG  # 网关日志
```

### 4. 数据库配置

如果数据库配置不同,修改各服务的application.yaml:

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_management
    username: your_username
    password: your_password
```

## 性能优化

### 1. JVM参数

生产环境启动时建议添加JVM参数:

```bash
java -jar -Xms512m -Xmx1024m -XX:+UseG1GC cm-auth-1.0-SNAPSHOT.jar
```

### 2. 连接池配置

在application.yaml中优化数据库连接池:

```yaml
spring:
  datasource:
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
```

## 监控方案

### 1. Nacos控制台

- 服务列表
- 服务详情
- 健康检查
- 服务流量

### 2. Spring Boot Actuator

访问各服务的actuator端点:

```
http://localhost:8081/actuator/health
http://localhost:8081/actuator/info
http://localhost:8081/actuator/metrics
```

## 下一步

1. ✅ 基础架构已搭建完成
2. ⏭️ 迁移业务代码到各微服务
3. ⏭️ 实现Feign接口调用
4. ⏭️ 添加网关认证过滤器
5. ⏭️ 集成监控告警

## 技术支持

如遇到问题,可以:

1. 查看Nacos日志: `nacos/logs/nacos.log`
2. 查看服务日志: 控制台输出或日志文件
3. 检查Nacos服务列表是否有异常服务
4. 使用Postman测试API接口

---

**祝开发顺利!** 🚀
