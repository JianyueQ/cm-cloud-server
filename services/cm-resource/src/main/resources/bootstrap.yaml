server:
  port: 8087
spring:
  application:
    name: cm-resource
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.127.140:8848
        namespace: dev
      config:
        server-addr: 192.168.127.140:8848
        namespace: dev
  config:
    import:
      - nacos:cm-resource.yaml?group=resource

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.cm.entity
  configuration:
    map-underscore-to-camel-case: true

