<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cm.mapper.AdminUserInfoMapper">

    <resultMap id="BaseResultMap" type="com.cm.vo.AdminUserInfoVO">
        <id column="id" property="id"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="email" property="email"/>
        <result column="avatar" property="avatar"/>
        <result column="gender" property="gender"/>
        <result column="status" property="status"/>
        <result column="user_type" property="userType"/>
        <association property="admin" javaType="com.cm.entity.Admin" column="id" select="getAdminInfoByUserId"/>
    </resultMap>

    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id">
        insert into user
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username != null and username != ''">username,</if>
            <if test="password != null and password != ''">password,</if>
            <if test="realName != null and realName != ''">real_name,</if>
            <if test="phone != null and phone != ''">phone,</if>
            <if test="email != null and email != ''">email,</if>
            <if test="userType != null">user_type,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="createUser != null">create_user,</if>
            <if test="updateUser != null">update_user,</if>
            is_deleted,
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="username != null and username != ''">#{username},</if>
            <if test="password != null and password != ''">#{password},</if>
            <if test="realName != null and realName != ''">#{realName},</if>
            <if test="phone != null and phone != ''">#{phone},</if>
            <if test="email != null and email != ''">#{email},</if>
            <if test="userType != null">#{userType},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="createUser != null">#{createUser},</if>
            <if test="updateUser != null">#{updateUser},</if>
            0,
        </trim>
    </insert>

    <insert id="insertAdmin">
        insert into admin(user_id, admin_no, position, hire_date, create_time, update_time, create_user, update_user, is_deleted)
        values(#{userId}, #{adminNo}, #{position}, #{hireDate}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, 0)
    </insert>

    <update id="updateUserInfo">
        update user
        <set>
            <if test="password != null and password != ''"> password = #{password}, </if>
            <if test="realName != null and realName != ''"> real_name = #{realName}, </if>
            <if test="phone != null and phone != ''"> phone = #{phone}, </if>
            <if test="email != null and email != ''"> email = #{email}, </if>
            <if test="status != null"> status = #{status}, </if>
            <if test="avatar != null and avatar != ''"> avatar = #{avatar}, </if>
            <if test="gender != null"> gender = #{gender}, </if>
        </set>
        where
            id = #{id} and is_deleted = 0
    </update>

    <update id="updateAdmin">
        update admin
        <set>
            <if test="position != null and position != ''"> position = #{position}, </if>
            <if test="hireDate != null"> hire_date = #{hireDate}, </if>
            <if test="updateTime != null"> update_time = #{updateTime},</if>
            <if test="updateUser != null"> update_user = #{updateUser},</if>
        </set>
        where
            user_id = #{userId} and is_deleted = 0
    </update>

    <update id="deleteUser">
        update user
        set is_deleted = 1
        where id in
        <foreach item="id" collection="list" separator="," open="(" close=")">
            #{id}
        </foreach>
    </update>

    <update id="deleteAdmin">
        update admin
        set is_deleted = 1
        where user_id in
        <foreach item="id" collection="list" separator="," open="(" close=")">
            #{id}
        </foreach>
    </update>

    <update id="updateUserInfoStatus">
        update user
        set status = #{status}
        where id = #{id} and is_deleted = 0
    </update>

    <select id="getAdminInfoByUserId" resultType="admin">
        select admin_no, position, hire_date
        from admin
        where user_id = #{id}
          and is_deleted = 0
    </select>

    <select id="getUserInfoById" resultMap="BaseResultMap">
        select id,real_name,phone,email,avatar,gender,status,user_type from user where id = #{currentId} and is_deleted = 0
    </select>

    <select id="selectById" resultType="com.cm.entity.User">
        select id, username, password, real_name, phone, email, avatar, gender, status, user_type
        from user where id = #{currentId} and is_deleted = 0
    </select>

    <select id="list" resultType="com.cm.vo.AdminWithUserInfoPageVO">
        select user.id, user.username, user.real_name, user.phone, user.email, user.status, admin.admin_no, admin.position
        from user left join admin on user.id = admin.user_id
        <where>
            <if test="username != null and username != ''">and user.username like concat('%', #{username}, '%')</if>
            <if test="realName != null and realName != ''">and user.real_name like concat('%', #{realName}, '%')</if>
            <if test="adminNo != null and adminNo != ''">and admin.admin_no like concat('%', #{adminNo}, '%')</if>
            <if test="status != null">and user.status = #{status}</if>
            and user.is_deleted = 0 and admin.is_deleted = 0 and user.id != 1
        </where>
        order by user.create_time desc
    </select>

    <select id="selectAdminWithUserInfoById" resultType="com.cm.vo.AdminWithUserInfoVO">
        select user.id, user.username, user.real_name, user.phone, user.email, user.status,user.create_time ,user.update_time ,admin.admin_no, admin.position ,admin.hire_date
        from user left join admin on user.id = admin.user_id
        where user.id = #{id} and user.is_deleted = 0 and admin.is_deleted = 0
    </select>

</mapper>